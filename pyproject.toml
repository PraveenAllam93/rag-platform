[project]
name = "rag-platform"
version = "0.1.0"
description = "Enterprise RAG Platform on AWS"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/*",
    "packages/*",
]